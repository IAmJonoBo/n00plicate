// deprecated - use `.eslintrc.cjs` (legacy) at project root that extends the workspace config.
// Flat config is disabled to avoid ESLint flat-config vs extends conflict. Use `.eslintrc.cjs` instead.
export default {};
