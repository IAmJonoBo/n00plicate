{
  "name": "n00plicate",
  "version": "0.1.1",
  "type": "module",
  "private": true,
  "description": "Design-token-driven pipeline using 100% libre tooling",
  "repository": {
    "type": "git",
    "url": "https://github.com/IAmJonoBo/n00plicate.git"
  },
  "license": "MIT",
  "author": "n00plicate Team",
  "workspaces": [
    "packages/*",
    "apps/*",
    "tools/*"
  ],
  "packageManager": "pnpm@10.21.0",
  "engines": {
    "node": ">=24.11.0",
    "pnpm": ">=10.21.0"
  },
  "scripts": {
  "build": "pnpm -r --filter '!n00plicate' --workspace-root=false --workspace-concurrency=1 --if-present build",
    "postbuild:clean": "echo 'Post-build cleanup skipped'",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:packages-node": "vitest run --project packages-node",
    "test:packages-browser": "vitest run --project packages-browser",
    "test:desktop": "vitest run --project desktop",
    "test:cli": "vitest run --project cli",
    "test:coverage": "vitest run --coverage",
    "test:shared-utils": "pnpm --filter shared-utils test",
  "test:affected": "pnpm -r --filter '!n00plicate' --workspace-root=false --workspace-concurrency=1 --if-present test",
    "lint:packages": "pnpm --filter design-tokens --filter design-system --filter shared-utils lint",
    "lint:base": "pnpm exec biome lint .",
  "lint:typed:packages": "pnpm exec biome check --reporter compact 'packages/token-*/**/*.{js,jsx,ts,tsx,mjs,cjs,cts,mts}' 'packages/tokens-*/**/*.{js,jsx,ts,tsx,mjs,cjs,cts,mts}' 'packages/ui-*/**/*.{js,jsx,ts,tsx,mjs,cjs,cts,mts}' 'packages/platform-bridges/**/*.{js,jsx,ts,tsx,mjs,cjs,cts,mts}' 'tools/**/*.{js,jsx,ts,tsx,mjs,cjs,cts,mts}'",
  "lint:typed:apps": "pnpm exec biome check --reporter compact 'apps/docs/**/*.{js,jsx,ts,tsx,mjs,cjs,cts,mts}' 'apps/workflows/**/*.{js,jsx,ts,tsx,mjs,cjs,cts,mts}' 'apps/examples/**/*.{js,jsx,ts,tsx,mjs,cjs,cts,mts}'",
  "lint:typed": "pnpm run lint:typed:packages && pnpm run lint:typed:apps",
  "lint:workspace": "pnpm run lint:base && pnpm run lint:md && pnpm run lint:css && pnpm run typecheck",
  "lint:affected": "pnpm -r --filter '!n00plicate' --workspace-root=false --workspace-concurrency=1 --if-present lint",
    "lint:css": "stylelint 'packages/**/*.{css,scss,sass}' --fix",
    "lint:md": "markdownlint-cli2 --config config.markdownlint-cli2.jsonc --fix \"docs/**/*.md\" \"packages/*/README.md\"",
    "lint:md:fix": "node scripts/fix-markdown-line-length.js && pnpm lint:md",
    "lint:md:fix-line-length": "node scripts/fix-markdown-line-length.js",
    "lint:commits": "commitlint --from=HEAD~1 --to=HEAD",
  "e2e": "pnpm -r --filter '!n00plicate' --workspace-root=false --workspace-concurrency=1 --if-present e2e",
  "dev": "pnpm -r --filter '!n00plicate' --workspace-root=false --workspace-concurrency=10 --if-present serve",
    "clean": "rm -rf node_modules && pnpm install",
    "clean:ds-store": "find . -name '.DS_Store' -type f -delete && find . -name '._*' -type f -delete",
    "clean:xcode": "find . -name '*.xcuserdata*' -type d -exec rm -rf {} + 2>/dev/null || true; find . -name 'xcuserdata' -type d -exec rm -rf {} + 2>/dev/null || true; find . -name '*.xcworkspace/xcuserdata' -type d -exec rm -rf {} + 2>/dev/null || true",
    "clean:spotlight": "find . -name '.Spotlight-V100' -type d -exec rm -rf {} + 2>/dev/null || true; find . -name '.fseventsd' -type d -exec rm -rf {} + 2>/dev/null || true",
    "clean:temp": "find . -name '*.tmp' -type f -delete 2>/dev/null || true; find . -name 'Thumbs.db' -type f -delete 2>/dev/null || true; find . -name '~*' -type f -delete 2>/dev/null || true",
    "clean:all": "pnpm run clean:temp && pnpm run clean",
  "clean:workspace": "pnpm -r --filter '!n00plicate' --workspace-root=false --workspace-concurrency=1 --if-present clean",
    "autofix": "./scripts/autofix.sh",
    "autofix:safe": "./scripts/autofix.sh",
    "format": "pnpm exec biome check --write --linter-enabled=false .",
    "format:check": "pnpm exec biome check --formatter-enabled=true --linter-enabled=false --assist-enabled=false --error-on-warnings .",
    "typecheck": "pnpm run typecheck:workspace",
  "typecheck:workspace": "pnpm -r --filter '!n00plicate' --workspace-root=false --workspace-concurrency=1 --if-present typecheck",
  "typecheck:nx": "pnpm -r --filter '!n00plicate' --workspace-root=false --workspace-concurrency=1 --if-present typecheck",
  "visual-test": "pnpm -r --filter '!n00plicate' --workspace-root=false --workspace-concurrency=1 --if-present visual-test",
    "storybook": "pnpm --filter design-system storybook",
    "build-storybook": "pnpm --filter design-system build-storybook",
    "tokens:build": "pnpm --filter design-tokens build",
  "tokens:build-all": "pnpm --filter design-tokens build && pnpm -r --filter '!n00plicate' --workspace-root=false --workspace-concurrency=1 --if-present build",
    "tokens:orchestrate": "cargo run --manifest-path packages/token-orchestrator/Cargo.toml --bin n00plicate-token-orchestrator",
    "tokens:sync": "pnpm run tokens:orchestrate && pnpm --filter design-tokens tokens:sync",
  "tokens:sync-all": "pnpm --filter design-tokens tokens:sync && pnpm -r --filter '!n00plicate' --workspace-root=false --workspace-concurrency=1 --if-present build",
    "tokens:watch": "pnpm --filter design-tokens watch",
    "tokens:validate": "pnpm run tokens:orchestrate -- --validate-only && node -e \"const { runTokenContractValidation } = require('./packages/shared-utils/dist/token-contract-validation.js'); runTokenContractValidation();\"",
    "tokens:check-drift": "echo '🔍 Checking for token drift...' && echo 'Drift check placeholder - would compare with Penpot'",
    "tokens:diff": "echo '📊 Token diff placeholder - would show changes between versions'",
    "tokens:monitor": "echo '👀 Token monitoring placeholder - would start continuous monitoring'",
    "tokens:cli:help": "echo '📋 Run n00plicate-token-cli without installing globally: pnpm dlx n00plicate-token-cli'",
    "tokens:cli:status": "echo '📋 Use: n00plicate-tokens status (run with: pnpm dlx n00plicate-token-cli)'",
    "validate:token-contract": "pnpm tokens:validate",
    "validate:penpot-export": "node -e \"console.log('🔍 Validating Penpot export...'); if (!require('fs').existsSync('tokens.json')) { console.log('❌ tokens.json not found'); process.exit(1); } console.log('✅ Penpot export validated');\"",
    "dev:full-stack": "concurrently 'pnpm --filter design-tokens watch' 'pnpm --filter design-system storybook'",
    "dev:all": "concurrently -n tokens,storybook,web 'pnpm tokens:watch' 'pnpm storybook' 'pnpm --filter web serve'",
  "test:e2e": "pnpm -r --filter '!n00plicate' --workspace-root=false --workspace-concurrency=1 --if-present e2e",
    "test:visual": "chromatic --exit-zero-on-changes",

  "graph": "echo 'Nx graph visualization removed; use a dependency graph tool (madge/custom script) or pnpm filters'",
  "graph:affected": "echo 'Affected graph detection: use a pnpm workspace filter or custom git-diff script'",
    "affected": "echo 'Affected commands not available without Nx'",
    "dep-graph": "echo 'Dependency graph not available without Nx'",
  "report": "echo 'Nx report deprecated; use workspace scripts and CI reporting'",
  "reset": "echo 'Nx reset deprecated; use pnpm store prune and phem to reset caches'",
  "repair": "echo 'Nx repair deprecated; follow workspace migration docs to repair'",
  "connect": "echo 'Nx connect removed; use workspace tooling or custom runner'",
  "watch": "echo 'Nx watch removed; use pnpm --filter <pkg> run watch'",
    "dev:runner": "node tools/dev-runner.js",
    "setup:dx": "./scripts/setup-dx.sh",
    "setup:complete": "pnpm install && pnpm build",
    "workspace:info": "echo 'Workspace info not available without Nx'",
    "analyze": "pnpm build && echo 'Analysis not available without Nx'",
    "security:gitleaks": "./scripts/run-gitleaks.sh detect --no-banner",
  "upgrade:plan": "echo 'Use pnpm upgrade or pnpm dlx npm-check-updates (npm-check-updates) to plan upgrades'",
    "upgrade:latest": "pnpm up --latest --recursive --workspace-root",
  "upgrade:verify": "pnpm install && pnpm -r --filter '!n00plicate' --workspace-root=false --workspace-concurrency=1 --if-present build,test,lint"
  },
  "devDependencies": {},
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json}": [
      "biome format --write",
      "biome check --write"
    ],
    "*.{js,jsx,ts,tsx}": [
      "biome check --write"
    ],
    "*.{html,astro}": [
      "prettier --write"
    ],
    "*.{css,scss,sass}": [
      "biome format --write"
    ],
    "*.{md,mdx}": [
      "node scripts/fix-markdown-line-length.js",
      "markdownlint-cli2 --config config.markdownlint-cli2.jsonc --fix"
    ],
    "*.{toml,yaml,yml,xml}": [
      "dprint fmt --stdin {}"
    ],
    ".github/instructions/nx.instructions.md": "skip"
  },

  "dependencies": {
    "@vanilla-extract/css": "1.17.4",
    "@vanilla-extract/vite-plugin": "5.1.1",
    "storybook": "10.0.7"
  }
}
